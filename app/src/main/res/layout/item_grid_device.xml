<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ll_recyc"
    android:layout_width="match_parent"
    android:layout_height="164dp"
    android:paddingBottom="10dp"
    android:background="@drawable/selector"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/image_updata"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:src="@drawable/device_new"
        android:visibility="gone" />


    <ImageView
        android:id="@+id/image_icon"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="35dp"
        android:src="@drawable/device_gw" />

    <com.heiman.widget.dragindicator.DragIndicatorView
        android:id="@+id/indicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/tv_txt"
        android:layout_alignEnd="@+id/image_icon"
        android:layout_alignRight="@+id/image_icon"
        android:layout_marginBottom="30dp"
        android:background="#ff0000"
        android:padding="6dp"
        android:textColor="#ffffff"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/tv_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/image_icon"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp"
        android:text="TextView01"
        android:textColor="#535353"
        android:textSize="14dp" />

    <TextView
        android:id="@+id/tv_state"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/tv_txt"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        android:text="TextView01"
        android:textColor="#999999"
        android:textSize="12dp" />
</RelativeLayout>