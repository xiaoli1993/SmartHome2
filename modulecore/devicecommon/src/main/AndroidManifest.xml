<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.heiman.devicecommon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CheckFirmwareUpgradeActivity" />
        <activity android:name=".OwnedRoomActivity" />
        <activity android:name=".ChoiceRoomActivity" />
        <activity android:name="com.heiman.devicecommon.ShareDeviceActivity" />
        <activity android:name=".TimerActivity"/>
    </application>

</manifest>